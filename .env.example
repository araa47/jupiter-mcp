# Jupiter Ultra MCP Server Environment Variables

# Solana RPC URL - Use a reliable RPC endpoint
# Free options: https://api.devnet.solana.com (devnet), https://api.mainnet-beta.solana.com (mainnet)
# Paid options: Helius, QuickNode, etc.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Private key for the Solana wallet (base58 encoded)
# This should be the private key of the wallet you want to use for transactions
# IMPORTANT: Keep this secret and never commit it to version control
PRIVATE_KEY=your_private_key_here

# Optional: Set to 'devnet' or 'mainnet-beta' (defaults to 'devnet')
SOLANA_NETWORK=mainnet-beta

# Optional: Timeout for HTTP requests in seconds (defaults to 30)
REQUEST_TIMEOUT=30
